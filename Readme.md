### **Depth Inference CLI Application**<br>

**Arguments**

1. `--mode | -m` - `image` or `realtime`
2. `--filename | -f` - Name of the image file (with extension)
3. `--save | -s` - Save the processed file

